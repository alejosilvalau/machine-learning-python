First run:
`pip install yfinance pandas pandas-ta scikit-learn langchain langchain-openai langchain-core`

Then create an `.env` file with the following content under the same directory as your script:
```env
GITHUB_TOKEN=github_fkajfl882i2....
```
